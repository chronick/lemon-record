/target
**/*.rs.bk
*.pdb
.DS_Store

# never commit recordings
*.wav
session-*/
recordings/

# release staging + regenerable icon intermediates
/dist/
icons/icon.iconset/

# never commit the updater PRIVATE key (the .pub is committed for embedding)
updater.key
updater.key.password
*.key
